const { calculate_sum } = require("./sum");
const { calculate_sub } = require("./sub");
const { calculate_mul } = require("./mul");
const { calculate_div } = require("./div");

module.exports = {
  calculate_sum,
  calculate_sub,
  calculate_mul,
  calculate_div,
};
